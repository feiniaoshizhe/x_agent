# ============================================
# Development Environment Variables
# ============================================
# generated by Forge
# DO NOT commit this file to version control
# ============================================

# ============================================
# Application Configuration
# ============================================
APP_NAME="x_agent"
APP_VERSION="1.0.0"
APP_DESCRIPTION="x_agent API"
DEBUG=false
ENV=development

# ============================================
# Database Configuration (MySQL)
# ============================================
DB_TYPE=mysql
DB_HOST=localhost
DB_PORT=3306
DB_DATABASE=x_agent
DB_USERNAME=root
DB_PASSWORD=123456
DB_CHARSET=utf8mb4

# Connection Pool Configuration
ECHO=true
POOL_PRE_PING=true
POOL_TIMEOUT=30
POOL_SIZE=6
POOL_MAX_OVERFLOW=2

# ============================================
# Authentication Configuration
# ============================================
JWT_SECRET_KEY=QENt1SoTwwlXcEa5kWVBaQGaxDoexh__qVYymr-kjTU
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRATION=1800
JWT_REFRESH_TOKEN_EXPIRATION=86400
JWT_ISSUER=x_agent
JWT_AUDIENCE=x_agent_users

# ============================================
# Email Configuration (SMTP)
# ============================================
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=your-email@gmail.com
EMAIL_HOST_PASSWORD=your-app-password
EMAIL_USE_TLS=true
EMAIL_USE_SSL=false
EMAIL_SSL_CERT_REQS=required
EMAIL_TIMEOUT=30
EMAIL_EXPIRATION=3600

# Email From Configuration
EMAIL_FROM_NAME="x_agent"
EMAIL_FROM_EMAIL=noreply@yourdomain.com

# ============================================
# CORS Configuration
# ============================================
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:8080,http://127.0.0.1:3000
CORS_ALLOW_CREDENTIALS=true
CORS_ALLOW_METHODS=GET,POST,PUT,DELETE,PATCH,OPTIONS
CORS_ALLOW_HEADERS=*
CORS_EXPOSE_HEADERS=

# ============================================
# Redis Configuration
# ============================================
REDIS_CONNECTION_URL=redis://localhost:6379
REDIS_POOL_SIZE=3
REDIS_SOCKET_TIMEOUT=10
REDIS_DEFAULT_TTL=3600

# ============================================
# Celery Configuration
# ============================================
CELERY_BROKER_URL=redis://localhost:6379/1
CELERY_RESULT_BACKEND=redis://localhost:6379/2
CELERY_TASK_SERIALIZER=json
CELERY_RESULT_SERIALIZER=json
CELERY_ACCEPT_CONTENT=["json"]
CELERY_TIMEZONE=UTC
CELERY_ENABLE_UTC=true
CELERY_TASK_ALWAYS_EAGER=false
CELERY_TASK_EAGER_PROPAGATES=true
CELERY_WORKER_CONCURRENCY=2
CELERY_WORKER_PREFETCH_MULTIPLIER=1
CELERY_RESULT_EXPIRES=3600

# Task routing (JSON format)
CELERY_TASK_ROUTES={}

# Periodic tasks schedule (configured in code)
# CELERY_BEAT_SCHEDULE is configured in app/core/celery.py

# ============================================
# Logging Configuration
# ============================================
LOG_LEVEL=DEBUG
LOG_TO_FILE=true
LOG_FILE_PATH=logs/app_development.log
LOG_TO_CONSOLE=true
LOG_CONSOLE_LEVEL=DEBUG
LOG_ROTATION=1 day
LOG_RETENTION_PERIOD=7 days

